# task.md — Prompt Studio

## Status Legend
- [ ] Pending  · [~] In Progress  · [x] Done  · [!] Blocked

Last updated: 2026-05-22
Live: https://yapweijun1996.github.io/Prompt-Studio/

---

## Phase 0 — Design & Decisions  ✅

- [x] DESIGN.md, docs/providers.md, docs/pwa.md
- [x] Research Gemini thinkingConfig + OpenAI reasoning.effort
- [x] Decided: TypeScript + agrun.js

## Phase 1 — Scaffold  ✅

- [x] Vite 6 + React 19 + TypeScript
- [x] Tailwind v4, Zustand, Dexie, vite-plugin-pwa, agrun (from GitHub)
- [x] vite-plugin-pwa configured (manifest, Workbox NetworkOnly for APIs)
- [x] .github/workflows/deploy.yml — GitHub Actions → Pages
- [x] PWA icons — generated by scripts/gen-icons.mjs

## Phase 2 — Gateway & XOR  ✅

- [x] xor.ts — encrypt/decrypt (key "20260515") + localStorage helpers
- [x] gateway.ts / openai.ts / gemini.ts / custom.ts adapters
- [x] convert.ts callProvider() router
- [x] Effort levels mapped per provider

## Phase 3 — State & Core Logic  ✅

- [x] Zustand store + Dexie schema
- [x] convert.ts — 3 parallel calls via Promise.allSettled
- [x] Per-variant system prompts (Creative/Balanced/Strict × 3)
- [x] Rate limit detection (429 → countdown)

## Phase 4 — UI  ✅

- [x] Header, InputPanel, OutputList/OutputCard, SettingsModal
- [x] OfflineBanner, RateLimitCountdown, PwaUpdateBanner
- [x] EffortSelector (in SettingsModal)

## Phase 5 — Light/Dark Theme  ✅

- [x] index.css — @custom-variant dark + @theme inline semantic tokens
- [x] No-flash inline script in index.html
- [x] Theme state in store (system-aware + persist)
- [x] ThemeToggle component
- [x] All components refactored to semantic tokens
- [x] Verified in Chrome — light + dark + modal

## Phase 6 — Deploy  ✅

- [x] GitHub Actions deploy succeeds
- [x] Live on GitHub Pages, PWA manifest + service worker served
- [x] PWA icons no longer 404

---

## Bug Fixes (from live testing)

- [x] **agrun null output** — `runAgrun()` surfaces `result.error` instead of
  crashing on `result.output.text` when a run fails
- [x] **session.run overhead** — switched to `runtime.run()` (one-shot) +
  `globalMemory: { enabled: false }`
- [x] **rate-limit not firing** — `convertPrompt()` returns `{ rateLimited }`
- [x] **Gemini budget overflow** — `clampBudget()` caps per model
- [x] **provider switch broken** — `setProvider()` now resets the model;
  `initialModel()` repairs already-persisted provider/model mismatches
  (Gemini was being called with `gpt-5.4-mini`)

---

## Open Items (not built)

- [ ] **TemplateManager UI** — Dexie schema (`db.ts`) ready; CRUD panel not built
- [ ] **PWA install button** — `beforeinstallprompt` capture not wired
- [ ] **Tests** — no test suite yet
- [ ] **Live provider E2E** — Default works; OpenAI/Gemini/Custom need a
  user key to verify end-to-end
- [ ] DESIGN.md "Open Questions" table is stale (JS/TS + agrun now decided)

---

## Known Risks

| Risk | Status |
|---|---|
| agrun runs planner + finalize per call → ~6 gateway calls per Convert | ~5 Converts/min under the 30/min limit; RateLimitCountdown handles 429 |
| XOR is reversible — demo key extractable | Accepted: rate-limited demo key |
| Demo key only works for Default provider | OpenAI/Gemini/Custom need the user's own key (Settings) |
